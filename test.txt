안녕하세요
경기 게마고 3기생
여러분 환영합니다!!

저장 단축키 : Ctrl + s(윈도우 프로그램은 다 이거다)

git-scm

ctrl + ~ (숫자키 1 왼쪽에 있어)
git init: 해당 폴더를 깃으로 관리하는 폴더로 초기화하는 명령

git config --global user.name "깃허브아이디"
git config --global user.email "깃허브이메일"

git add test.txt

git commit -m "최초의 커밋입니다"

두번째로 수정을 해봅시다.
아무거나 아무거나
git add . 그냥 다 들어가게함

git log

git checkout 넘버4개
git checkout master <= 마스터 브랜치의 최신버전으로 돌아갈래
아무말이나 변경하시고 add commit을 수행하는데 커밋메시지는
퀴즈해결로 git lob 확인창을 보여주면 됩니다.
하이하이
aa

vs 코드에서 커밋시에는 ctrl + enter치면 커밋이 됩니다.
git remote add origin 주소

git remote -v
asdfasdfasdf 또ㅓ 변경합니다